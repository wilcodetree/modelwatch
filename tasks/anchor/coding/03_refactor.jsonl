{"id":"coding.refactor","version":"1.0.0","area":"coding","input":"Work in /workspace. Remove the duplicated line-total calculation from invoice_total without changing observable results. Run pytest -q, then submit only the unified diff for your change.","target":"The duplicated calculation is extracted and all existing tests keep the same outcome.","metadata":{"scorer":"sandbox_pytest,no_em_dash","fixture":"fixtures/repo_refactor","known_good":"--- a/pricing.py\n+++ b/pricing.py\n@@ -1,21 +1,15 @@\n from decimal import Decimal\n \n \n+def _line_total(line: dict[str, object]) -> Decimal:\n+    quantity = int(line[\"quantity\"])\n+    unit_price = Decimal(str(line[\"unit_price\"]))\n+    discount = Decimal(str(line.get(\"discount\", \"0\")))\n+    return unit_price * quantity * (Decimal(\"1\") - discount)\n+\n+\n def invoice_total(lines: list[dict[str, object]]) -> Decimal:\n     total = Decimal(\"0\")\n     for line in lines:\n-        quantity = int(line[\"quantity\"])\n-        unit_price = Decimal(str(line[\"unit_price\"]))\n-        if line.get(\"taxable\", True):\n-            subtotal = unit_price * quantity\n-            discount = Decimal(str(line.get(\"discount\", \"0\")))\n-            subtotal = subtotal * (Decimal(\"1\") - discount)\n-            total += subtotal\n-        else:\n-            subtotal = unit_price * quantity\n-            discount = Decimal(str(line.get(\"discount\", \"0\")))\n-            subtotal = subtotal * (Decimal(\"1\") - discount)\n-            total += subtotal\n+        total += _line_total(line)\n     return total.quantize(Decimal(\"0.01\"))\n","known_bad":"--- a/pricing.py\n+++ b/pricing.py\n@@ -18,4 +18,4 @@\n             subtotal = subtotal * (Decimal(\"1\") - discount)\n             total += subtotal\n-    return total.quantize(Decimal(\"0.01\"))\n+    return (total + Decimal(\"1\")).quantize(Decimal(\"0.01\"))\n","since":"2026-09-23"}}
