{"id":"coding.pytest_fix","version":"1.0.0","area":"coding","input":"Work in /workspace. Fix the inventory defect demonstrated by the failing test. Run pytest -q, then submit only the unified diff for your change.","target":"The full available quantity can be reserved and the complete test suite passes.","metadata":{"scorer":"sandbox_pytest,no_em_dash","fixture":"fixtures/repo_pytest_fix","known_good":"--- a/src/inventory.py\n+++ b/src/inventory.py\n@@ -65,7 +65,7 @@\n         self._require_sku(sku)\n         if quantity <= 0:\n             raise InventoryError(\"reserved quantity must be positive\")\n-        if quantity >= self.available(sku):\n+        if quantity > self.available(sku):\n             raise InventoryError(\"insufficient available stock\")\n         self._reserved[sku] += quantity\n         return self._reserved[sku]\n","known_bad":"--- a/src/inventory.py\n+++ b/src/inventory.py\n@@ -66,7 +66,7 @@\n         if quantity <= 0:\n             raise InventoryError(\"reserved quantity must be positive\")\n         if quantity >= self.available(sku):\n-            raise InventoryError(\"insufficient available stock\")\n+            raise InventoryError(\"not enough stock\")\n         self._reserved[sku] += quantity\n         return self._reserved[sku]\n","since":"2026-09-23"}}
