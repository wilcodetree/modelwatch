# modelwatch v0.1, Claude Code session prompts (Sonnet)

One prompt per step, one fresh session per step, never two steps in one conversation. Model:
Sonnet for all six steps (mechanics against a decided spec). Switch to Opus only for a question
the spec does not answer, after the session has stopped and Wilco has agreed.

Runs in: a Claude Code session on the laptop, cwd `C:\ZND\projects\modelwatch`, `main` at the
previous step's tag. Paste one block. API keys are in the user environment before the session
starts (`.env.example` lists the names).

## Step 1 prompt (skeleton, hello task, flatten) week 42

```text
Read C:\ZND\AGENTS.md, then C:\ZND\projects\modelwatch\AGENTS.md, then C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md and do Step 1 only. Create pyproject.toml managed by uv with inspect-ai pinned to the release installed today (read the version from the package, write it into SESSION_LOG.md), pyyaml, typer, httpx, pandas, pytest; run uv sync. Build src\modelwatch\cli.py with the verbs run, import, flatten, report, selftest, roster (import and report as stubs that print "not in this step"). Build tasks\hello.py (three exact-match items), config\roster.yaml with two entries: one Anthropic model direct and one OpenRouter model with a pinned upstream provider, both as exact dated snapshot IDs read today from the vendor model pages, no aliases. Build flatten.py per section 0 of the spec and the selftest checks listed in Step 1. Run the hello task on both models, flatten, and show me the six rows. Done when six rows are in results\results.ndjson and results\results.sqlite, uv run pytest is green with a fixture .eval log test, uv run modelwatch selftest is green. House rules: no em dashes, full C:\ZND\... paths in prose. Never read C:\dev\Work or any Valona folder. If the spec leaves a choice open, stop and ask me with two or three candidates and a recommendation. Stop before tagging or pushing; print the tag command for v0.1.0-alpha.1 and add one paragraph to SESSION_LOG.md.
```

## Step 2 prompt (public layer importers, dashboard v0) week 43

```text
Read C:\ZND\AGENTS.md, C:\ZND\projects\modelwatch\AGENTS.md and C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md, then do Step 2 only. Build src\modelwatch\external\epoch.py, aa.py, livebench.py and metr.py as idempotent importers writing source=external rows per section 0. Before writing each importer, fetch the live documentation or data URL named in the spec and confirm the column or field names today; where the spec says "whichever the session finds documented", use what you find and note it in SESSION_LOG.md. Store the licence and attribution string of each source in the notes column. Build report.py with the dashboard v0: self-contained HTML, Chart.js vendored under src\modelwatch\static, no CDN, one chart per external series, a table of latest values, and a "no private runs yet" section. Done when uv run modelwatch import --all returns rows for at least six current models, a second import the same day adds zero rows, reports\dashboard.html opens with four charts, uv run pytest has a fixture test per importer, selftest is green. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask with candidates and a recommendation. Stop before tagging; print the tag command for v0.1.0-alpha.2 and add a SESSION_LOG.md paragraph.
```

## Step 3 prompt (anchor batch 1: hub edits and injection) week 44

```text
Read C:\ZND\AGENTS.md, C:\ZND\projects\modelwatch\AGENTS.md and C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md, then do Step 3 only. Write the fixture hub under tasks\fixtures\hub_small\ fresh for the fixture, copy nothing from C:\ZND\10_holding. Write the 7 hub_edits items and the 5 injection items as jsonl per section 0, each with known_good and known_bad completions in metadata, and the deterministic scorers listed in Step 3 under src\modelwatch\scorers\deterministic.py. Add selftest --tasks that scores every known_good and known_bad. Add the error-bar computation to report.py (SE over repeats, paired difference between two models per item). Run the 12 items on the two roster models with 3 repeats and show me the per-area means with SE. Done when all 12 items pass selftest --tasks, the dry run is in the store, the report shows means with error bars, pytest is green. Task text stays in the repo: do not paste item text into this chat beyond the item ids. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask. Stop before tagging; print the tag command for v0.1.0-alpha.3 and add a SESSION_LOG.md paragraph. End by telling me, per item, whether the two models differ by more than one SE, for gate 1.
```

## Step 4 prompt (anchor batch 2: voice and Dutch, judge, calibration) week 45

```text
Read C:\ZND\AGENTS.md, C:\ZND\projects\modelwatch\AGENTS.md, C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md Step 4, and for the rubric C:\ZND\10_holding\00_company\brand\voice_and_writing_style.md plus the my-writing-style skill. Write the 6 voice items (3 NL, 3 EN) and the 4 dutch items with known_good and known_bad completions. Build scorers\judge.py: cross-vendor judge chosen from config\judges.yaml by the provider under test, five-point rubric, scored twice with positions swapped, mean recorded, judge_model recorded, disagreement above 0.3 noted. Build scorers\wordlists.py from the humanize skill's banned-word list with today's date. Build the calibrate verb: write 10 judged completions to reports\calibration_<date>.md with blank score cells, and calibrate --read to compute agreement within one point and append to 03_logs\calibration.ndjson. Run the dry run on the two roster models, generate the calibration file and stop so I can score it; then continue on my say-so with calibrate --read. Done when 10 items pass selftest --tasks (good above 0.8, bad below 0.4), agreement is at least 8 of 10, dry run is in the store, pytest is green. Below 6 of 10: stop and propose rubric changes, do not proceed. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask. Stop before tagging; print the tag command for v0.1.0-alpha.4 and add a SESSION_LOG.md paragraph.
```

## Step 5 prompt (anchor batch 3: Docker coding and skill following) week 46

```text
Read C:\ZND\AGENTS.md, C:\ZND\projects\modelwatch\AGENTS.md and C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md Step 5. Confirm Docker Desktop is running (docker info) before anything else. Read the installed inspect-ai version's documentation for its sandbox and default agent API names and use those; note them in SESSION_LOG.md. Write five fixture repos under tasks\fixtures\repo_<name>\ each with a compose.yaml, written fresh, no real ZND code. Write the 5 coding items and the 3 skill-following items with known_good and known_bad as patches, and the scorers listed in Step 5. Apply the Step 5 limits (token_limit 60000, time_limit 900 s, message limit 40) and confirm the spend guard fires on a deliberately low test threshold before the real dry run. Run the 8 items on the two roster models once. Done when all 8 items pass selftest --tasks, the dry run completes under EUR 10 total, docker ps -a shows no modelwatch containers afterwards, pytest is green. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask. Stop before tagging; print the tag command for v0.1.0-alpha.5 and add a SESSION_LOG.md paragraph.
```

## Step 6 prompt (full run 1, report, post draft) week 47

```text
Read C:\ZND\AGENTS.md, C:\ZND\projects\modelwatch\AGENTS.md and C:\ZND\projects\modelwatch\02_roadmap\2026-09-22_v0.1_spec.md Step 6. First build the roster: open the model pages of Anthropic, OpenAI, Google, xAI and Artificial Analysis today, fill config\roster.yaml with ten exact dated snapshot IDs per the Step 6 list (two Anthropic, two OpenAI, one Gemini, one Grok, two open-weight via OpenRouter with pinned upstream, one of my choice which I will name when you ask, one local Ollama floor), and fill config\prices.yaml in EUR per million tokens from the vendor price pages, both files dated today; show me both files and wait for my ok. Then run uv run modelwatch run --all --roster config\roster.yaml --repeats 5 model by model under the EUR 300 guard, and report progress per model with running cost. Build the run report per Step 6 (dot-and-whisker per area, paired-difference table, cost and wall time, external values alongside, footer with versions) and the dashboard's private time series, and report --post-draft in house voice with area names only, no item text. Done when run 1 is in the store for all ten models or a guard stop is recorded and explained, the report renders, the post draft exists, pytest and selftest are green. House rules from C:\ZND\AGENTS.md. Open choice: stop and ask. Stop before tagging; print the tag command for v0.1.0 and add a SESSION_LOG.md paragraph. End with, per area, whether any pair of models differs by more than one SE, for gate 2.
```

## After each step (PowerShell on the laptop, Wilco)

```powershell
# runs in: PowerShell on the laptop. Replace the tag and message with the ones the session printed.
cd C:\ZND\projects\modelwatch
git add -A
git commit -m "v0.1.0-alpha.1: skeleton, hello task, flatten to ndjson and sqlite"
git tag v0.1.0-alpha.1
git push origin main --tags
```
